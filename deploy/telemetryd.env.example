# Keep permissions at 0600.
TELEMETRYD_TOKEN=replace-with-a-long-random-token

# systemd expands this into separate ExecStart arguments.
TELEMETRYD_OPTIONS=--grpc-listen=:80 --http-listen=127.0.0.1:8080 --ready-requires-stream=true --bn-stale-after=3m --rn-stale-after=5m --log-json=true
